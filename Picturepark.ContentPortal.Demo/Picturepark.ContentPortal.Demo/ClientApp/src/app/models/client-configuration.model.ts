export interface ClientConfiguration {
  contactEmail: string;
  apiServer: string;
  identityServer: string;
  redirectUrl: string;
  customerId: string;
  clientId: string;
  scope: string;
  customerAlias: string;
}
